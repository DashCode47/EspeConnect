export interface BannerRow {
  id: string;
  title: string;
  description: string;
  imageUrl: string;
  image_url?: string;
  isActive: boolean;
  is_active?: boolean;
  createdAt: string;
  created_at?: string;
}
